# 现状报告 · Angel_ICE

> 一句话：**地基已经稳了（能动、能看见、不说谎），但上层"目标层"是空的 —— 她不会持续发育。**

| 项 | 值 |
|---|---|
| 报告时间 | 2026-09-25 14:07 (GMT+8) |
| 运行状态 | ⏸ **全部停止**（用户要求停止本地迭代） |
| 自测 | **774/774 全绿** |
| 台账 | 48 条问题，177 029 字节 |
| 代码量 | 8 个核心模块共 **817 315 字节** |
| Git | ⚠️ **零提交** |

---

## 一、现在她能做什么（实测）

### ✅ 已经能做到

| 能力 | 证据 |
|---|---|
| **自主决策** | 656 秒无人工干预，持续 tick（452 tick），有决策日志 |
| **自主尝试生存** | 日志实测：反复尝试 `shelter` / `forage` / `hunt` |
| **失败退避** | `⏸ forage 连续失败 9 次，退避 90s（附近 5 格内没有能打的猎物）` |
| **看见远处** | `/scan` `maxDistance: 22.9`、`distinct: 14`，能看见 14.2 格外的绿宝石矿 |
| **感知提速** | 分档心跳：挖矿 ×0.5 / 战斗 ×0.3 / 待机 ×2，最快 **450 ms** 刷新 |
| **自我脱困** | `POST /unstick` + 三级自救（放行 canDig → 挖脚边 → 如实放弃） |
| **不说谎** | P47 修完后，失败会明确报 `success: false` |
| **用掉材料** | 背包 `dirt` 从 22 → 13（**她真的放了 9 个土去建庇护所**） |

### ❌ 做不到（**主线目标**）

| 用户要求 | 实际情况 |
|---|---|
| 「**自己**建立庇护所」 | ❌ `needShelter` 只在**天黑**时为 true —— 白天永不主动规划 |
| 「**持续**发育」 | ❌ `matStacks 21 > 8` 达标 → 停止采集 → 发育**静止** |
| 「发育」 | ❌ `canEquipTool: false`、`itemKinds: 1` —— **没有任何工具** |

**她的实际状态**：
```
位置   (1.36, 67, 24.5)
背包   dirt × 13          ← 只有土
血量   18
物品种类 1                ← 无工具、无食物、无木头
```

**为什么不能发育 —— 三条缺口**：

```
① needShelter = "天黑了"（decision.js:574）
   语义是"应急"，不是"建立" → 白天不搭房子

② needMaterials 只看"堆数"不看"种类"（decision.js:127）
   selfMatStacks: 8，她是 21 个 dirt → 达标 → 停采
   ⚠️ "21 个土" ≠ "21 个能用的材料"

③ 没有工具链目标
   capability 观测了 canEquipTool，但决策层没有动作消费它
   forage 与 hasFoodSource 循环依赖：
     没食物源 → 不觉得饿 → 不去找食物 → 永远没食物源
```

---

## 二、本轮修了什么（10 条）

| 编号 | 问题 | 状态 |
|---|---|---|
| P40 | `/shelter` 12 次全失败（选址假设错） | ✅ |
| P41 | `shelter` 判据用 `success` 而非 `placed` | ✅ |
| P42 | 被困在 1 格高缝隙（`canDig=false` 永久困死） | ✅ 三级自救 |
| P43 | 掉落物"报告 y" ≠ "可站层" | ✅ |
| P44 | `/move` 报成功但纹丝未动 | ✅ |
| P45 | `/scan` 看不见远处（真瓶颈不是慢） | ✅ |
| P46 | `/unstick` 逃逸目标在实心岩体内 | ✅ |
| **P47** | **路由层无条件贴 `success: true`**（四次同型根因） | ✅ |
| P48 | 不会持续发育（**主线未达成**） | 📋 已批准，未实现 |
| P49 | `/craft` 报错混淆"缺材料"与"没配方" | ✅ 诊断已加 |

### 最重要的故事：P47（架构级）

**一个 bug 长了四次，我前三次都只改了判据字段。**

```
P32  /pickup   walkedTo:3 但背包没变     → 我加了 picked
P35  /gather   同型                      → 我加了 mined
P41  /shelter  一块没放也报成功          → 我改了 placed
P44  /move     纹丝未动却报成功          → 我加了 moved / wasInside
                                              ↑ 四次都在改"判据"，
                                                四次都没问"为什么调用方会信 success"
```

**根因只有一行**：
```js
json(res, 200, { success: true, ...result });   // 无条件
```
它只说"handler 没抛异常"，但**所有调用方读成"动作生效了"**。

**修完的实机效果对比**：

```
旧：挖 bedrock 挖不动 → {"success":true,"mined":0,...}  ← 上层以为挖到了
新：挖 bedrock 挖不动 → {"success":false,"mined":0,"ok":false,"note":"搜到 128 格仍未拿够"}
新：挖 andesite 挖到  → {"success":true,"ok":true,"mined":1}  ← 不误伤
```

### 最有价值的教训：P45（我把问题问错了）

| 我的第一判断 | 实测结果 |
|---|---|
| "`/scan` 太慢" | **8~12 ms，一点也不慢** ❌ |

真症状：`radius=8` 与 `radius=16` **返回完全一样**（`scanned:1536, distinct:6`）
—— **她永远只能看见 7 格、6 种方块，一个矿都没有**。

**如果以"优化速度"为目标收工，完全修不到真问题。**

修完 A/B：

| 指标 | 旧 | 新 |
|---|---|---|
| `radius=16` `maxDistance` | **7.16（永远）** | **22.9** |
| 看见矿吗 | **0 个** | **绿宝石 / 煤 ×17 / 盐** |
| 耗时 | 8~12 ms | 8~15 ms |

---

## 三、我在本轮犯的错（**交接时必须知道**）

### 3.1 P49：**我连续误判了三次**

排查 `/craft` 报 `No recipe for coarse_dirt` 时：

| 轮次 | 我的判断 | 事实 |
|---|---|---|
| ① | "配方表没加载" | ❌ 我读的 `bot.recipes` 属性**在 mineflayer 4.x 根本不存在** |
| ② | "minecraft-data 缺 1.20.1 的 recipes.json" | ❌ 数据**好好的有 729 条** |
| ③ | 真相：`coarse_dirt` 配方是 **dirt×2 + cobblestone×2**，她只有土没圆石 | ✅ `No recipe` **完全正确** |

**连我自己都栽在"没有 vs 读不到"上** —— 而这是本项目最核心的设计原则。

**已修的**：
- 新增 `GET /recipes` 诊断端点，用 `recipesAll`（只查配方）+ `recipesFor`（查背包）
  **配对**，把三种情况一刀切开
- `/craft` 报错改成：`材料不够（配方有 1 条，但一条都做不了）：dirt 0/2 + gravel 0/2`

### 3.2 P40：**错误的心算被当成实测数据**

我"心算"出 `Math.round(-6.5) = -7`，据此推导出一个自相矛盾的结论，还拿它当证据。
**实际 `Math.round(-6.5) = -6`（向 +∞ 取整）。**

### 3.3 多次"差点去改正确的代码"

P45 自测 5 条红、P42 自测 4 条红，**根因都是我的断言写错了**（`check` 是严格相等，
不能比对象），而**代码逻辑是好的**。

**教训：红的时候先怀疑断言。**

---

## 四、发现的**新问题**（未修）

### 🔴 P50：`AIRY` 判据不含植物

```js
const AIRY = /^(air|cave_air|void_air|water|flowing_water|lava|flowing_lava)$/;
```

实机：她站在 `(1, 67, 24) = grass`（`solid: false`，**可穿过的草**），
但被判 `selfAir = false` → `/shelter` 报"脚边没有可放置的空位"。

**判定确实错了**（草当然能站进去）。但**改动有风险**：`AIRY` 被**两处**使用：
- **放置目标**：需要"能被替换" → 草可以 ✅ 该加
- **她站位**：需要"她能站" → 草能站 ✅ 该加

⚠️ **动之前先查清两件事**：
1. `grass` 是模组方块名（可能对应 `short_grass`）
2. **`lava` 已经在 `AIRY` 里** —— 岩浆不能站，为什么在"可站立"判据里？
   （这是 P40 遗留的设计疑问，`DEADLY` 正则里有 `lava` 在兜底）

### 🟡 现实约束：**附近没有树**

实测扫描（radius 24）：只有 `grass_block / dirt / andesite / limestone /
煤矿 / 盐矿 / 铜矿`，**零个木质方块**。

**这对"工具链"是硬约束**：Minecraft 工具链的第一步是"徒手采木"，
而**当前地点周围没有树**。

→ 工具链的第一步不该是"采木"，而应该是"**先找到树**"（需要探索/远距离寻路），
或者由玩家带她到有树的地方。

---

## 五、她的位置变化（本轮）

```
本轮开始  (-10.5, 87, -7.5)   背包 dirt × 21
本轮结束  (1.36, 67, 24.5)    背包 dirt × 13
```

- **水平移动 ~34 格，垂直下降 20 格** —— 她确实在走动、挖掘、尝试
- **背包从 21 → 13** —— 用掉 9 个土（建庇护所的尝试）
- 她曾**被玩家带到当前位置**（`Ka_sum1` 在 4 格内，陪在身旁）

---

## 六、健康状况

| 项 | 状态 |
|---|---|
| 自测 | ✅ **774/774** |
| 语法检查 | ✅ 全部通过 |
| 崩溃/未捕获异常 | ✅ 无 |
| 台账完整性 | ✅ 48 条，每条含症状/定位/修法/教训 |
| **Git** | ⚠️ **零提交 —— 所有工作未落盘** |
| 版本号 | ⚠️ 不一致（1.11.0 vs 1.0.0） |
| 文档同步 | ⚠️ `SKILL.md` / `README.md` 滞后 |

---

## 七、结论

**做对的**：
- 本轮 10 条修复全部是**根因级**，且都有自测锁定
- P47 是真正的架构修复，覆盖所有端点
- 建立了"**先量再改**"的纪律（P45 靠它翻案）
- 台账 48 条是可传承的资产

**没做到的**：
- **用户主线目标"自己建立庇护所并持续发育"未达成** —— 三条设计缺口（P48）已定位、
  方案 C 已批准，但**未实现**
- Git 零提交 —— 本轮所有工作**面临丢失风险**
- P50 判定缺口未修

**最紧急的两件事**：
1. **`git commit`**（否则工作会丢）
2. **实现方案 C**（用户主线的唯一路径），且要先解决"附近没树"
